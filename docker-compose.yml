services:
  mongo:
    image: mongo:7
    container_name: airtel-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - airtel_mongo_data:/data/db

volumes:
  airtel_mongo_data:
